<form method="post" action="{{ path('app_admin_setting_delete', {'id': setting.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ setting.id) }}">
    <button  class="btn btn-block btn-danger">Delete</button>
</form>
